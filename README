deno run --allow-all --watch --import-map=./import_map.json ./src/main.ts

Create car devuelve el coche anterior en caso de ya existir uno porque "estas intentando meter algo que no sabes que ya tienes"